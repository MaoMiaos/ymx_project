CREATE TABLE `user` (
                        `id` VARCHAR(50) NOT NULL PRIMARY KEY,
                        `user_name` VARCHAR(20),
                        `password` VARCHAR(20),
                        `name` VARCHAR(20)
) COMMENT '用户表';